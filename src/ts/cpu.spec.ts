import { expect } from 'chai';
import 'mocha';

/**
 * @todo TDD ...
 */
describe('cpu test cases (getNextMove)', () => {
  it('should correctly return valid square index', () => {
    expect(true).to.be.false;
  });
});
